import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Create Position",
};

export default function CreatePositionLayout({ children }: { children: React.ReactNode }) {
	return children;
}
